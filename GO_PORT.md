# Go port

The Go rewrite of Context Runtime lives in its own repo:
**https://github.com/redevops-io/context-runtime-go**

This Python implementation is the source of truth — develop + test features here first,
then propagate them to the Go port. See that repo's `REWRITE_PLAN.md` for the tiered
port plan and status.
