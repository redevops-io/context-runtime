"""Context Runtime integrations — using the runtime as an efficiency optimizer for real apps.

Each integration treats a host app's decision point (what context to surface) as a
plan, and the app's own success metric as the reward. The first tenant is sidekick;
the same shape generalizes to any app with a (decision point → measurable outcome).
"""
